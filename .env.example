# Document API Configuration
# Copy to .env and adjust values as needed.

# SQLite database file path (default: ./documents.db)
# DATABASE_URL=./documents.db

# Upload directory (default: ./uploads)
# UPLOAD_DIR=./uploads

# API key for authentication. Leave unset to disable auth (dev mode).
# API_KEY=your-secret-api-key

# CORS allowed origins, comma-separated. Leave empty to block all cross-origin.
# CORS_ORIGINS=http://localhost:3000,https://myapp.com

# Max upload file size in bytes (default: 10485760 = 10 MB)
# MAX_FILE_SIZE=10485760

# Logging level: DEBUG, INFO, WARNING, ERROR (default: INFO)
# LOG_LEVEL=INFO

# Debug mode: show detailed error messages in 500 responses (default: false)
# DEBUG=false

# Secret key for CSRF token signing. Change this in production!
# CSRF_SECRET=your-random-secret-here
